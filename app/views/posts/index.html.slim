h1 Posts

- @tags.each do |tag|
  = link_to "#{tag.name}", post_filter_path(tag.name), :class => "filter-by-tag"

table
  thead
    tr
      th Author
      th Title
      th Body
      th Tags
      th
      th

  tbody
    - @posts.each do |post|
      tr id = "post-row-#{post.id}"
        td class="post-author" = post.author
        td class="post-title" = post.title
        td class="post-body" = post.body
        td class="post-tags" = post.tag_strings.map {|c| c.name}.join(', ')
        td = link_to 'Edit', edit_post_path(post)
        td = link_to 'Destroy', post, data: {:confirm => 'Are you sure?'}, :method => :delete

br
